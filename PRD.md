# PRD — 우리 동네 카페 지도 서비스

> 엑셀로 정리한 카페 목록을 업로드하면, 주소를 좌표로 변환해 카카오맵에 마커로 표시하고,
> 로그인한 사용자가 각 카페의 방문 여부와 한줄 소감을 기록·관리할 수 있는 웹 서비스.

- 문서 버전: v0.1 (초안)
- 작성일: 2026-07-27
- 상태: 검토 대기 (코드 미착수 — 문서 확정 후 개발 시작)

---

## 1. 개요 (Overview)

동네 카페 탐방을 즐기는 사용자가 자신이 직접 정리한 카페 리스트(엑셀)를 그대로 지도로 옮겨 보고,
방문 기록과 감상을 남길 수 있게 하는 것이 목표다. 별도의 DB 입력 화면 없이 "엑셀 업로드 →
지도에 자동 표시"라는 흐름을 핵심 경험으로 삼는다.

- **핵심 가치**: 내 손으로 만든 카페 목록이 곧바로 나만의 지도가 되고, 방문할 때마다 소감이 쌓인다.
- **1차 사용자**: 카페 탐방 리스트를 엑셀로 관리하는 개인 사용자.

---

## 2. 목표와 비목표 (Goals / Non-Goals)

### 2.1 목표 (Goals)
- 엑셀 파일을 업로드하면 카페 목록(이름·주소·카테고리)을 읽어들인다.
- 주소를 좌표로 변환하여 카카오맵 위에 마커로 표시한다.
- 좌표를 찾지 못한 주소는 화면에 별도로 안내한다.
- 마커를 클릭해 방문 여부 체크와 한줄 소감을 입력·저장한다.
- 회원가입/로그인 후 본인의 소감을 저장하고, 재방문 시 이전 기록을 그대로 다시 보여준다.
- 방문 체크한 카페만 모아보는 별도 목록 화면을 제공한다(엑셀 없이, 로그인 본인 것만).

### 2.2 비목표 (Non-Goals, 이번 범위 아님)
- 카페 정보 크롤링·자동 수집(엑셀 업로드로만 목록 확보).
- 다른 사용자의 소감 공유·소셜·댓글 기능.
- 카페 검색/필터 고도화(카테고리 필터 정도만 향후 검토).
- 모바일 네이티브 앱(반응형 웹으로 대응).
- 결제·예약·리뷰 평점 집계.

---

## 3. 사용자 & 시나리오 (Personas & Scenarios)

- **탐방러(주 사용자)**: 가보고 싶은 카페 목록을 엑셀로 관리한다. 엑셀을 올려 지도로 보고,
  방문할 때마다 마커를 눌러 "다녀옴" 체크와 한줄 소감을 남긴다. 나중에 "내가 다녀온 카페"만
  다시 훑어본다.

대표 시나리오
1. 엑셀 업로드 → 지도에 마커가 뜬다. 못 찾은 주소는 안내 영역에 뜬다.
2. 마커 클릭 → 방문 여부 체크 + 한줄 소감 입력 → 저장.
3. 다음에 로그인 후 같은 카페 마커를 누르면 예전 소감이 그대로 보인다.
4. "방문한 카페" 화면에서 엑셀 없이도 내가 다녀온 곳만 모아본다.

---

## 4. 핵심 기능 요구사항 (Functional Requirements)

### F1. 엑셀 업로드 및 목록 읽기
- 사용자가 엑셀 파일(.xlsx)을 업로드하면 클라이언트에서 파싱한다.
- 필수 컬럼: **이름(name)**, **주소(address)**, **카테고리(category)**.
- 헤더 매핑/필수 컬럼 누락 시 사용자에게 오류를 안내한다.
- 파싱 결과는 화면(지도)에 반영하기 위한 임시 목록으로 사용한다.
  (엑셀 원본 목록 자체를 서버에 영구 저장할지는 [Q1] 참조.)

### F2. 지오코딩 및 마커 표시
- 각 카페 주소를 카카오맵(로컬) API로 좌표(위도/경도)로 변환(지오코딩)한다.
- 변환된 좌표에 마커를 찍고, 마커에 카페 이름/카테고리를 노출한다.
- **좌표를 못 찾은 주소는 지도에 찍지 않고**, 화면의 "표시 실패 목록" 영역에
  (이름 + 주소 + 실패 사유)로 안내한다.
- 카카오맵 호출은 **프론트엔드에서 직접** 수행한다.

### F3. 마커 클릭 → 방문 체크 + 한줄 소감
- 마커 클릭 시 정보창/패널에서 다음을 입력·수정한다.
  - 방문 여부(visited: 예/아니오)
  - 한줄 소감(memo, 텍스트) — 길이 제한 [Q2].
- 로그인 상태에서만 저장 가능. 비로그인 시 로그인 유도.

### F4. 회원가입/로그인 & 소감 영속화
- Supabase Auth 기반 회원가입/로그인(이메일 기준, 방식은 [Q3]).
- 저장된 소감은 본인 계정에 귀속된다.
- 같은 카페 마커를 다시 클릭하면, 저장돼 있던 방문 여부·소감이 그대로 보인다.
- **동일 사용자 + 동일 장소(이름 + 주소)에 대한 기록은 1개만 유지된다**
  (재저장은 신규 생성이 아니라 기존 기록 갱신 = upsert). → 6장 데이터 모델 참조.

### F5. 방문한 카페 모아보기
- "방문 체크한 카페"만 모아 목록 화면으로 제공한다.
- **엑셀 업로드 없이도** 조회 가능(저장된 본인 데이터 기반).
- RLS로 **로그인한 본인의 데이터만** 조회된다.
- 목록 항목: 이름, 주소, 카테고리, 한줄 소감, (선택) 저장 일시.

### F6. 방문 현황 요약 표시
- 지도 위(상단)에 **"총 ○곳 중 ○곳 방문 완료"** 형태의 요약을 표시한다.
- **총 ○곳**: 현재 지도에 표시 중인 카페 수(엑셀 업로드 → 지오코딩 성공분 기준).
- **방문 ○곳**: 그중 방문 체크(visited=true)된 카페 수.
  - 방문 여부는 로그인한 본인의 저장 기록(F4)을 지도 목록에 덧입힌 결과를 기준으로 센다.
  - 따라서 **비로그인 상태에서는 방문 수가 0**으로 표시된다(개인 기록이 반영되지 않으므로).
- 방문 체크를 저장하면(F3/F4) 요약 수치가 **즉시 갱신**된다(팝업을 다시 열지 않아도 반영).
- 표시할 카페가 하나도 없으면(엑셀 미업로드 등) 요약을 감추거나 "표시할 카페가 없어요" 정도로 안내한다.

### F7. 방문 여부에 따른 마커 색 구분
- 지도 마커를 **방문 완료(visited=true)** 와 **미방문**으로 **색을 다르게** 표시한다.
- 방문 여부 판정 기준은 F4/F6과 동일하다: 로그인한 본인의 저장 기록(cafe_notes)을
  지도 목록에 덧입힌 결과의 `visited` 값. 매칭 키는 **이름 + 주소**.
  - 따라서 **비로그인 상태에서는 모든 마커가 미방문 색**으로 표시된다(개인 기록 미반영).
- 방문 체크를 저장하면(F3/F4) 해당 마커 색이 **즉시 갱신**된다(팝업을 다시 열지 않아도 반영).
- 마커를 다시 그릴 때는 이전 마커를 모두 지운 뒤 새로 그린다(색 갱신도 이 규칙을 따른다).
- 두 색은 지도 배경 위에서 서로 구분되게 하고, 어떤 색이 방문/미방문인지 알 수 있도록
  간단한 범례(또는 요약 영역 안내)를 함께 제공한다.

---

## 5. 시스템 구성 & 기술 스택 (Architecture)

- **프론트엔드**: 웹앱(React 기반 가정 — [Q4]로 확정). 반응형.
- **지도**: 카카오맵 JavaScript SDK + 로컬(주소→좌표) API. **프론트엔드에서 직접 호출**.
- **인증/DB**: Supabase (Auth + Postgres Database).
- **행 수준 보안**: Supabase RLS로 사용자별 데이터 격리.
- **배포**: Vercel.

### 5.1 키/환경변수 관리
- 카카오맵 JavaScript 키는 클라이언트에 노출되는 특성을 감안한다.
- 로컬 개발: `.env` 파일로 관리(레포에 커밋 금지, `.gitignore` 등록).
- 배포: **Vercel 환경변수**로 주입.
- 클라이언트 노출 변수는 프레임워크 규칙에 맞는 접두어 사용
  (예: Vite `VITE_`, Next.js `NEXT_PUBLIC_`) — 프레임워크 확정 후 정리([Q4]).
- **보안 조치**: 카카오 개발자 콘솔에서 도메인 화이트리스트(허용 도메인) 등록으로 키 오남용 방지.
- Supabase는 `anon` public key만 클라이언트에 두고, 데이터 보호는 RLS로 강제한다.
  (`service_role` 키는 클라이언트/레포에 절대 포함하지 않음.)

권장 환경변수(초안)
```
VITE_KAKAO_MAP_KEY=...            # 카카오맵 JS 키 (클라이언트 노출, 도메인 제한 필수)
VITE_SUPABASE_URL=...             # Supabase 프로젝트 URL
VITE_SUPABASE_ANON_KEY=...        # Supabase anon key (RLS로 보호)
```

---

## 6. 데이터 모델 & 보안 (Data Model & RLS)

### 6.1 테이블 (초안): `cafe_notes`
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK) | 기록 식별자 |
| user_id | uuid | 소유자 (auth.users 참조) |
| name | text | 카페 이름 |
| address | text | 카페 주소 |
| category | text | 카테고리 |
| visited | boolean | 방문 여부 |
| memo | text | 한줄 소감 |
| created_at | timestamptz | 생성 시각 |
| updated_at | timestamptz | 수정 시각 |

### 6.2 "1인 1장소 1기록" 제약
- **동일 사용자 + 동일 장소(이름 + 주소)** 조합은 유일해야 한다.
- 구현: `UNIQUE (user_id, name, address)` 제약 + 저장 시 **upsert**(있으면 갱신, 없으면 생성).
- 결과적으로 같은 사람이 같은 카페(이름+주소)에 남긴 기록은 항상 1개만 유지된다.
- 장소 동일성 판단 기준의 정규화(공백/대소문자/주소 표기 차이 처리)는 [Q5] 참조.

### 6.3 RLS (행 수준 보안)
- `cafe_notes`에 RLS 활성화.
- 정책: 본인 행만 `select / insert / update / delete` 허용 (`auth.uid() = user_id`).
- 이로써 F5의 "본인 것만 조회", F4의 "본인 소감만 접근"이 DB 레벨에서 강제된다.

---

## 7. 화면 구성 (Screens)
1. **로그인/회원가입 화면**
2. **메인 지도 화면**: 엑셀 업로드 버튼 + **방문 현황 요약("총 ○곳 중 ○곳 방문 완료")** + 지도(**방문/미방문 색이 구분된 마커** + 범례) + "표시 실패 목록" 영역
3. **마커 상세 패널**: 방문 체크 + 한줄 소감 입력/조회
4. **방문한 카페 목록 화면**: 본인 방문 기록 리스트

---

## 8. 예외 & 엣지 케이스 (Edge Cases)
- 엑셀 필수 컬럼 누락/빈 값/중복 행 처리.
- 지오코딩 실패 주소 안내(F2) 및 대량 실패 시 UX.
- 지오코딩 호출량이 많을 때의 처리(속도/쿼터) — [Q6].
- 비로그인 상태에서 저장 시도 시 처리(로그인 유도).
- 같은 세션에서 서로 다른 엑셀을 연속 업로드할 때의 지도 초기화 정책.

---

## 9. 성공 지표 (Success Metrics, 초안)
- 엑셀 업로드 → 지도 표시 성공률(지오코딩 성공 비율).
- 마커 클릭 대비 소감 저장 전환율.
- 재방문 시 이전 소감 정상 노출률(F4 신뢰성).

---

## 10. 확정 필요 질문 (Open Questions — 코드 착수 전 확인)
- **[Q1] 엑셀 원본 목록 저장 여부**: 업로드한 전체 카페 목록을 서버에 저장할지, 아니면
  방문 체크/소감을 남긴 카페만 DB에 저장할지? (현재 초안은 "기록 남긴 것만 저장" 가정)
- **[Q2] 한줄 소감 제한**: 글자 수 상한(예: 100자)과 이미지/이모지 허용 여부?
- **[Q3] 로그인 방식**: 이메일+비밀번호만? 소셜 로그인(카카오/구글) 포함?
- **[Q4] 프론트엔드 프레임워크**: React(Vite) vs Next.js? (환경변수 접두어·배포 방식에 영향)
- **[Q5] 장소 동일성 기준**: "이름+주소"의 정규화 규칙(공백/대소문자/도로명·지번 혼용) 처리 방침?
- **[Q6] 지오코딩 전략**: 업로드 시 일괄 변환 캐싱 여부, 카카오 API 무료 쿼터 한도 대응?
- **[Q7] 엑셀 포맷 고정 여부**: 컬럼명/순서를 고정 템플릿으로 강제할지, 유연 매핑 UI를 둘지?
```
