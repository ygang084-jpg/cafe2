# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 현재 상태 (중요)

이 저장소는 **코드 작성 전 기획 단계**다. 현재 존재하는 것은 `PRD.md`(제품 요구사항 문서)와
`.vscode/settings.json`뿐이며, 애플리케이션 소스·빌드 스크립트·테스트·의존성 파일은 아직 없다.

- 작업을 시작하기 전에 반드시 `PRD.md`를 먼저 읽는다. 이 프로젝트의 유일한 사양(source of truth)이다.
- 빌드/린트/테스트 명령은 아직 존재하지 않는다. 스택이 확정(아래 "열린 결정")되면 이 문서에 추가한다.
- **PRD.md 10장의 [Q1]~[Q7]이 미해결이다.** 이 결정들은 아키텍처에 직접 영향을 주므로,
  관련 코드를 짜기 전에 사용자에게 먼저 확인한다.

## 무엇을 만드는가

"우리 동네 카페 지도 서비스" — 엑셀 카페 목록(이름·주소·카테고리)을 업로드하면 주소를 좌표로
변환해 카카오맵에 마커로 표시하고, 로그인 사용자가 카페별 방문 여부와 한줄 소감을 기록·조회하는
반응형 웹 서비스. 상세 기능은 PRD.md의 F1~F5 참조.

## 계획된 아키텍처

전체 흐름은 여러 파일에 걸쳐 이해해야 하므로 큰 그림을 여기 정리한다.

- **지도 = 프론트엔드 전용 경로**: 카카오맵 SDK와 지오코딩(주소→좌표)은 **프론트엔드에서 직접**
  호출한다. 백엔드 프록시를 두지 않는다. 지오코딩에 실패한 주소는 지도에 찍지 않고 화면의
  "표시 실패 목록"에 안내한다 (이 실패 경로는 핵심 요구사항이며 생략 금지).
- **데이터 = Supabase 전용 경로**: 인증(Supabase Auth)과 소감 저장(Supabase Postgres)은
  Supabase로 처리한다. 별도 백엔드 서버가 없으므로 **데이터 격리는 애플리케이션 코드가 아니라
  RLS(행 수준 보안)로 DB에서 강제**한다. `auth.uid() = user_id` 정책으로 본인 행만 접근 가능.
  클라이언트에는 `anon` 키만 두고, `service_role` 키는 절대 클라이언트/저장소에 포함하지 않는다.
- **핵심 데이터 불변식**: 한 사용자가 같은 장소(**이름 + 주소**)에 남기는 기록은 **항상 1개**다.
  `cafe_notes` 테이블의 `UNIQUE(user_id, name, address)` + 저장 시 **upsert**(신규 insert가 아닌
  기존 갱신)로 구현한다. 재방문 시 이전 소감이 그대로 다시 보이는 동작(F4)이 여기에 의존한다.
- **엑셀은 표시용, DB는 기록용**: 엑셀 업로드는 지도에 뿌릴 임시 목록을 만드는 용도다.
  "방문한 카페 모아보기"(F5)는 **엑셀 없이도** 저장된 본인 기록만으로 동작해야 한다.

계획된 데이터 모델(초안)은 PRD.md 6장의 `cafe_notes` 테이블 정의를 따른다.

## 키 · 환경변수 규약

- 카카오맵 JS 키는 클라이언트에 노출되므로, 카카오 개발자 콘솔에서 **허용 도메인 화이트리스트**로
  보호한다. 키 자체를 숨기려 하지 않는다.
- 로컬은 `.env`(저장소 커밋 금지, `.gitignore` 등록), 배포는 **Vercel 환경변수**로 주입한다.
- 클라이언트 노출 변수는 프레임워크 접두어 규칙을 따른다(Vite `VITE_`, Next.js `NEXT_PUBLIC_`).
  실제 접두어는 프레임워크 확정 후 결정된다([Q4]).

## 반드시 지킬 규칙
- 카카오맵 키는 `.env`의 `VITE_KAKAO_MAP_KEY`로 관리한다.
- 카카오맵 SDK는 `libraries=services`를 포함해서 불러온다
  (주소→좌표 변환에 필요).
- Supabase URL과 anon key는 `.env`의 `VITE_SUPABASE_URL`,
  `VITE_SUPABASE_ANON_KEY`로 관리한다.
- 소감 테이블은 RLS를 켜고 "본인 데이터만" 정책을 적용한다.
- `.env`는 `.gitignore`에 포함한다.
- 화면은 가짜 데이터로 먼저 만들고, 마지막에 실제 연동으로 교체한다.
- 마커를 다시 그릴 때는 이전 마커를 모두 지운 뒤 새로 그린다.
- 주소→좌표 변환은 하나씩 순서대로, 실패는 화면에 안내한다.
- 소감 저장은 upsert로, 같은 장소(이름+주소) 기록은 1개만 유지한다.
- Supabase 관련 작업(테이블, RLS)은 Supabase MCP 도구로 수행한다.

## 기술 스택
React + Vite, Tailwind + shadcn/ui, xlsx, 카카오맵 JS SDK,
Supabase(Auth/Database, MCP로 관리)
